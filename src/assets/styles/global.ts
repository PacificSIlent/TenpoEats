import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  page: {
    paddingHorizontal: 0,
    alignItems: 'center',
    flexGrow: 1,
    height: '100%',
    width: '100%',
  },
});
